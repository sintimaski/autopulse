# Builds and uploads the lumonox-sdk distribution (Python import package: ``lumonox``).
#
# Runs on pushes to ``main`` that touch SDK sources or metadata (see ``paths``), but only uploads
# when ``sdk/pyproject.toml`` ``version`` is not yet on PyPI—so bump the version to cut a release.
#
# Manual: Actions → Publish lumonox-sdk to PyPI → Run workflow (same PyPI-version guard).
# Maintainer checklist: docs/runbooks/LUMONOX_PYPI_RELEASE_CHECKLIST.md
#
# One-time PyPI setup: Project ``lumonox-sdk`` → Publishing → trusted publisher for this workflow
# (see https://docs.pypi.org/trusted-publishers/). Create GitHub Environment ``pypi`` if you use
# protection rules.
name: Publish lumonox-sdk to PyPI

on:
  push:
    branches: [main]
    paths:
      - "sdk/pyproject.toml"
      - "sdk/src/**"
      - "sdk/README.md"
      - "sdk/LICENSE"
  workflow_dispatch:

concurrency:
  group: lumonox-sdk-pypi-publish
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/project/lumonox-sdk/
    permissions:
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - name: Decide whether to publish
        id: decide
        run: |
          set -euo pipefail
          VERSION=$(python3 -c "import tomllib; print(tomllib.load(open('sdk/pyproject.toml', 'rb'))['project']['version'])")
          echo "sdk_version=${VERSION}"
          CODE=$(curl -sS -o /dev/null -w '%{http_code}' "https://pypi.org/pypi/lumonox-sdk/${VERSION}/json")
          echo "pypi_version_probe_http=${CODE}"
          if [ "${CODE}" = "200" ]; then
            echo "publish=false" >> "${GITHUB_OUTPUT}"
            echo "::notice::Skipping PyPI upload: lumonox-sdk ${VERSION} already exists on PyPI. Bump sdk/pyproject.toml version to release."
          elif [ "${CODE}" = "404" ]; then
            echo "publish=true" >> "${GITHUB_OUTPUT}"
            echo "::notice::Will publish lumonox-sdk ${VERSION} to PyPI."
          else
            echo "::error::Unexpected PyPI response (${CODE}) for lumonox-sdk ${VERSION}; not publishing."
            exit 1
          fi

      - name: Install uv
        if: steps.decide.outputs.publish == 'true'
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Build sdist + wheel (hatchling)
        if: steps.decide.outputs.publish == 'true'
        run: uv build --package lumonox-sdk --out-dir dist

      - name: Verify wheel contains lumonox package
        if: steps.decide.outputs.publish == 'true'
        run: |
          set -euo pipefail
          whl="$(ls dist/*.whl | head -1)"
          unzip -l "$whl" | grep -Fq "lumonox/__init__.py"

      - name: Upload to PyPI
        if: steps.decide.outputs.publish == 'true'
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/
